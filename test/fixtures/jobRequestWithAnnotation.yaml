apiVersion: platform.publishing.service.gov.uk/v1
kind: JobRequest
metadata:
  name: govuk-replatform-test-app
  annotations:
    platform.publishing.service.gov.uk/requested-by: foo
spec:
  containerFrom:
    podSpecFrom:
      group: apps/v1
      kind: Deployment
      name: govuk-replatform-test-app
    containerName: app
  command: rake
  args: [ "hello" ]
